# ClientServerFlaskReact

To compile client 

```
cd client
npm install --save-dev
chmod +x compile.sh
./compile.sh
```

To start server
```
cd server
pip3 install -r requirements.txt
python3 flask server
```
